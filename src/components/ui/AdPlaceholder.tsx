"use client";

export function CarbonAd() {
  // Placeholder — actual Carbon Ads integration requires approval + unique ID
  return null;

  /* Real implementation (after Carbon Ads approval):
  return (
    <div className="my-8 p-4 bg-gray-50 border border-gray-200 rounded-xl">
      <div className="text-xs text-gray-400 text-center mb-2">Advertisement</div>
      <Script
        src="//cdn.carbonads.com/carbon.js?serve=YOUR_CARBON_ID&placement=fixmebuttoncom"
        async
      />
    </div>
  );
  */
}
